// Chapter -06 if...else Statements

/*
What is if...else?
An if...else statement is used to make decisions in a program.
It executes one block of code if the condition is true, otherwise it executes another block.

if (condition) {
     Executes if condition is true
} else {
    Executes if condition is false
}
*/

let age = 20;
if (age >= 18) {
    console.log("eligible to vote");
} else {
    console.log("Not eligible");
}

// indise constion we use comparision operator 